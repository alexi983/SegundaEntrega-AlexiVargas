---
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<Header />

<section class="py-16 px-4 md:px-20 text-gray-800">
  <div class="max-w-3xl mx-auto bg-white shadow-lg rounded-lg p-8">
    <h1 class="text-3xl md:text-4xl font-bold text-blue-900 mb-6 text-center">
      Servicios On Line &gt; Pago de Permiso de Circulacion
    </h1>
    <h2 class="text-2xl font-semibold mb-4 text-center">Identificacion</h2>
    <form id="permisoForm" class="space-y-6">
      <div>
        <label for="rut" class="block text-left text-gray-700 font-medium mb-1">R.U.T.:</label>
        <input id="rut" type="text" placeholder="ej.: 12345678-K"
          class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-800 focus:outline-none" />
      </div>
      <div>
        <label for="patente" class="block text-left text-gray-700 font-medium mb-1">Patente Vehicular:</label>
        <input id="patente" type="text" placeholder="ej.: AZ1234"
          class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-800 focus:outline-none" />
      </div>
      <div class="flex justify-center mt-6">
        <button type="submit"
          class="bg-blue-800 text-white font-semibold px-8 py-3 rounded-lg hover:bg-blue-700 transition">
          Consultar
        </button>
      </div>
    </form>
    <p id="mensaje" class="text-center text-red-600 font-semibold mt-6 hidden"></p>
    <div class="mt-10 text-sm text-gray-600 leading-relaxed">
      <p><strong>NOTA:</strong> Servicio valido solo para contribuyentes con permiso de circulacion anterior pagado en CHONCHI.</p>
      <p>Los contribuyentes que deseen hacer traslado de vehiculo a la comuna de Chonchi deben enviar sus antecedentes al correo:</p>
      <p class="mt-2 font-semibold text-blue-800">permisosdecirculacion@municipalidadchonchi.cl</p>
      <p class="mt-4 font-bold text-blue-900">CHONCHI.</p>
    </div>
    <div class="text-center mt-10">
      <button id="btn-volver" class="bg-gray-600 text-white font-semibold px-8 py-2 rounded hover:bg-gray-500">
        Volver
      </button>
    </div>
  </div>
</section>

<Footer />

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const form = document.getElementById("permisoForm");
    const mensaje = document.getElementById("mensaje");
    const volver = document.getElementById("btn-volver");
    if (form) {
      form.addEventListener("submit", function (e) {
        e.preventDefault();
        const rutInput = document.getElementById("rut");
        const patenteInput = document.getElementById("patente");
        const rut = (rutInput as HTMLInputElement)?.value.trim() || "";
        const patente = (patenteInput as HTMLInputElement)?.value.trim() || "";
        if (rut === "" || patente === "") {
          if (mensaje) {
            mensaje.textContent = "Debe ingresar RUT y Patente para continuar.";
            mensaje.classList.remove("hidden");
            mensaje.classList.remove("text-green-600");
            mensaje.classList.add("text-red-600");
          }
        } else {
          if (mensaje) {
            mensaje.textContent = "Verificando informacion...";
            mensaje.classList.remove("hidden");
            mensaje.classList.remove("text-red-600");
            mensaje.classList.add("text-green-600");
            setTimeout(function () {
              mensaje.textContent = "";
              mensaje.classList.add("hidden");
            }, 2500);
          }
        }
      });
    }

    if (volver) {
      volver.addEventListener("click", function () {
        window.history.back();
      });
    }
  });
</script>
